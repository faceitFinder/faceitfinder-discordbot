version: '3'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    restart: on-failure
    container_name: faceitfinder
    command: npm start
    volumes: 
      - ./:/usr/src/app
    env_file:
      - .env